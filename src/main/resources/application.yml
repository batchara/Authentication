spring:
  profiles:
    servlet:
      multipart:
        max-file-size: 50MB

  springdoc:
    api-docs:
      enabled: true
    swagger-ui:
      path: /swagger-ui.html
      enabled: true
    default-produces-media-type: application/json

  server:
          servlet:
            context-path: /api/v1/


  datasource:
    url: jdbc:postgresql://localhost:5432/TriCo
    username: postgres
    password:
    driver-class-name : org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update

    show-sql: false


    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: raoudatebatcha@gmail.com
    password: ttlvtvybkfpfqbrm
    properties:
      mail:
        smtp:
          trust: "*"
          auth: true
          starttls:
            enable: true
          connectiontimout: 5000
          timeout: 3000
          writetimeout: 5000


application:
  security:
    jwt:
      secret-key: cetteCleSecreteDoitFaireAuMoins32Caracteres!!
      expiration: 8640000

  mailing:
    frontend:
      activation-url: http://localhost:4200/activate-account

debug: true
